import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ecoHog.com',
  appName: 'Economista Hogareño',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
